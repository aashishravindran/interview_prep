"""
k most frequent array in a sorted array 
partition array in odd and even parts 
merge sort
implementation of heap
""
# //Sum({1 <Year = { '$( =$(vLastYear) )' }>}  [Value])
# year= {'$(=$(vLast))'}
"""

# def reverse(nums,left):
#             right=len(nums)-1
#             while left<right:
#                 temp=nums[left]
#                 nums[left]=nums[right]
#                 nums[right]=temp
#                 left+=1
#                 right-=1
             
               
#         right=len(nums)-2
        
#         while right>=0 and nums[right+1]<=nums[right]:
#             right-=1
            
        
        
#         if right>=0:
#             new_right=len(nums)-1
#             while new_right>=0 and nums[new_right]<=nums[right]:
#                 new_right-=1
            
#             nums[right],nums[new_right]=nums[new_right],nums[right]
            
        
#         reverse(nums,right+1)
                    
                    
#                 

# Reverse a linked list from position m to n. Do it in one-pass.

# Note: 1 ≤ m ≤ n ≤ length of list.

# Input: 1->2->3->4->5->NULL, m = 2, n = 4
# Output: 1->4->3->2->5->NULL


